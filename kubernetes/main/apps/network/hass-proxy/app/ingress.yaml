---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hass-proxy
  annotations:
    external-dns.alpha.kubernetes.io/target: external.monosense.io
spec:
  ingressClassName: external
  rules:
    - host: &host ha.monosense.io
      http:
        paths:
          - backend:
              service:
                name: hass-proxy
                port:
                  name: http
            path: /
            pathType: Prefix
  tls: [hosts: [*host]]