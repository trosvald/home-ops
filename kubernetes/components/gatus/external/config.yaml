endpoints:
  - client:
      dns-resolver: tcp://8.8.8.8:53
    conditions:
      - '[STATUS] == ${GATUS_STATUS:=200}'
    group: external
    interval: 1m
    name: ${APP}
    url: https://${GATUS_SUBDOMAIN:=${APP}}.${GATUS_DOMAIN:=monosense.io}${GATUS_PATH:=/}
