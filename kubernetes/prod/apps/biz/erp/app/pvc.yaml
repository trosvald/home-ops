apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: erp-addons
  namespace: biz
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: ceph-block
