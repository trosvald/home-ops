---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/source.toolkit.fluxcd.io/gitrepository_v1.json
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: dev-kubernetes
  namespace: flux-system
spec:
  interval: 30m
  url: ssh://git@github.com/trosvald/home-ops
  ref:
    branch: main
  secretRef:
    name: github-deploy-key
  ignore: |
    # exclude all
    /*
    # include flux directories
    !/kubernetes/dev
---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: dev-cluster
  namespace: flux-system
spec:
  interval: 30m
  path: ./kubernetes/dev/flux
  prune: true
  wait: false
  sourceRef:
    kind: GitRepository
    name: dev-kubernetes
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: dev-cluster-settings
        optional: false
      - kind: Secret
        name: dev-cluster-secrets
        optional: false
